<%#= form_for([@post, @post.comments.build]) do |f| %>
<%= form_for(@post, url: post_comments_path(@post)) do |f| %>

<div class="field">
  <div class="control">
    <%#= f.text_field :name, input_html: { class: 'input' }, wrapper: false, label_html: { class: 'label' } %>
  </div>
</div>

<div class="field">
  <div class="control">
    <%= f.text_field :comments, input_html: { class: 'textarea' }, wrapper: false, label_html: { class: 'label' }  %>
  </div>
</div>
<div class="actions">
        <%= f.submit "Add comment" %>
      </div>
<% end %>